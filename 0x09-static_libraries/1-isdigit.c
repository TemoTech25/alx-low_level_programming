#include "main.h"
            	
/**
 * _isdigit - look for a digit (0 through 9)
 * @c: int to be checked
 *
 * Return: 1 c is digit, other wise 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
