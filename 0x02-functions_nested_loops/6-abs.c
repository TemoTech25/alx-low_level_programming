#include "main.h"

/**
 * _abs - computes the absolute value of an int value
 *
 * @n: the int value must be check
 *
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
